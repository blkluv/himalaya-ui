import { Layout, Playground, Attributes } from 'lib/components'
import { Button, Spacer, Text, TIP } from 'components'

export const meta = {
  title: 'LUV NFT TIP',
  group: 'Pay',
}

# LUV NFT TIP

## Purpose

Send crypto to anyone, even if they don't have a wallet. No app needed!  

## How

1. Buy crypto with a debit card from Stripe <a href="https://crypto.link.com/?ref=lb" target="_blank">here</a>.  

2. Visit https://tip.luvft.com and create a Tip link.


<Attributes edit="/pages/web5/luv-nft-tip.mdx">
<Attributes.Title>TIP</Attributes.Title>

```ts
type BodyScrollOptions = {
  delayReset: number // resume scrolling after a delay, default is 0
}

const TIP = (
  elementRef?: RefObject<HTMLElement> | null,
  options?: BodyScrollOptions,
) => [boolean, Dispatch<SetStateAction<boolean>>]
```

</Attributes>
